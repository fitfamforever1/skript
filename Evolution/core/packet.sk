import:
    org.bukkit.Particle

on packet event play_server_world_particles:
    set {_p} to field 9 of event-packet
    if event-packet.getNewParticles().read(0).getParticle() is Particle.DAMAGE_INDICATOR:
        cancel event